function fibonacci(layers) {
    if (layers < 2) return layers;

    return fibonacci(layers - 2) + fibonacci(layers - 1);
}

// print time();
write("This is a line.");

// let start = clock();
// print start;
// print clock() - start;
// print fibonacci(2);
// print clock() - start;