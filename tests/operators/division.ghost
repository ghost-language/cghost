let x = 10;

Assert.equals(x / 10, 1);
Assert.equals(10 / x, 1);
Assert.equals(x / x, 1);
Assert.equals(x / -1, -10);

Assert.equals(x / 2.5, 4);
Assert.equals(20 / 2.5, 8);

Assert.equals(x % 2, 0);
Assert.equals(x % 3, 1);