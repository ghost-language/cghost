print("Hailstone sequence");
let start = clock();
let n = 27;

while (n != 1) {
    if (n - 2 * (n / 2) == 0) {
        n = n / 2;
    } else {
        n = 3 * n + 1;
    }
}

let end = clock();

print("elapsed:");
print(end - start);